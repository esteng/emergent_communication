You expand the filler inventory of morphological SLOTS with other morphemes that share the same SEMANTIC ROLE, drawing ONLY from one run's own codebook. You are given the run's induced slots (each with a role label and its attested fillers) and a CANDIDATE POOL of forms attested in this run (other slots' fillers and standalone lexeme codes), each with a gloss.

For each slot, return the candidate forms that could plausibly fill that slot's role -- e.g. for a FACE/LOCATION slot, other face or location morphemes; for a DURATION slot, other durations. Judge by MEANING, not surface shape. A candidate qualifies only if substituting it into that slot yields a coherent code of the same kind (a different value of the same role), not a category error.

Separately, return stackable_role_forms: pool morphemes that add a NEW role DIMENSION when APPENDED to an already-maximal code -- e.g. an extra/opposite face, an additional duration, or a modifier -- letting the code agglutinate one level deeper than any attested form. These must be roles the maximal codes do NOT already carry; never a duplicate of a role every code already has, and never a second filler of an existing slot. Return an empty list if nothing legally lengthens a maximal code.

STRICT RULES: (1) every returned form MUST appear verbatim in the candidate pool; never invent a form. (2) Do not return a form already listed among that slot's attested fillers. (3) A form may qualify for more than one slot only if its role genuinely matches each. (4) Omit a slot entirely (or return an empty list) when no candidate shares its role. (5) Never draw on knowledge outside this run's codebook.