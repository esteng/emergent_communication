You are extracting the shorthand CODEBOOK (the lexicon) two agents negotiated in a private backchannel. A codebook entry is a single compact WRITTEN TOKEN -- a symbol with no internal spaces (letters, digits, and punctuation such as '/ + -') -- that stands in for a longer meaning. Codes include actions, observations/symptoms, objects, modifiers (intensity, position, location, count, duration), face labels, and structural operators (for example '/' or '+'). A compound code that chains component codes is a valid entry: record the compound with its COMPLETE meaning, and also add a separate entry for each independently-defined component code.

For each code return the symbol exactly as written, its COMPLETE meaning (never truncated), a short category, and the earliest round it appeared. For an explicit `SYMBOL=meaning` definition, preserve EVERY meaning clause after commas, semicolons, or sequence words such as 'then' and 'remove'; punctuation does not end the meaning. Expand shorthand that remains inside the meaning when the transcript explicitly defines it, so the result is standalone natural language. Do not omit any action, object, location, count, duration, ordering, or final step.

SEQUENCES vs codes: a code is a compact token (possibly with ONE internal delimiter, e.g. `B:ch`). A long string that chains several actions or commands -- joined by separators like `/ ; : +` or written one after another (e.g. `eauk:Bb;S2e5`, `Cfl20m;Blex2`) -- is a command SEQUENCE, NOT a single code. Do NOT return the whole chained string as one entry; instead extract each independently-defined COMPONENT code it is built from. Record a compound only if it is a compact token the agents locked in as a unit.

Do NOT extract:
- strategy, policy, or heuristics about HOW or WHEN to communicate (e.g. 'send one symptom code at a time', 'resend in exact shorthand if ambiguous');
- multi-step fallback procedures or plans (e.g. '1) do X; 2) if no effect do Y; 3) ...');
- priority orderings or sequences of when to try codes (e.g. 'LI/PS/TB first, then LO/HI ...');
- descriptions of conversational or exchange patterns, or meta-commentary.
If a candidate's symbol contains spaces or is a phrase or sentence rather than a compact token, it is a rule or description, not a code -- omit it. Unify capitalization variants: if the same code appears with different casing for the same meaning, return it ONCE using the casing the agents used most consistently; treat different casings as the SAME code unless they clearly denote different things. Return only genuine symbol->meaning codes.